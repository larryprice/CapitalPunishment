development:
  sessions:
    default:
      database: dev
      hosts:
        - localhost
      options:
        consistency: :strong
  options:

test:
  sessions:
    default:
      database: test
      hosts:
        - localhost